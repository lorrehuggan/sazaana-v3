import Link from 'next/link';
import { styled } from '../../../stitches.config';
export const A = styled(Link, {
  // Reset
  appearance: 'none',
  border: 'none',
  margin: 0,
  padding: 0,
  background: 'none',
  // Custom
});
